{{#partial "contenido"}}
<h1>Pois</h1>
<table class="table table-stripped">
	<tr>
		<th>Nombre</th>
		<th>Tipo</th>
		<th></th>
	</tr>
	
	{{#each pois}}
		<tr>
			<td>{{nombre}}</td>
			<td>{{tipo}}</td>
			<td><a href="{{url}}">Editar</a><td>
			<form class="form-inline" action="eliminar/{{url}}" method="POST">
			<button type="submit" class="btn btn-default">Eliminar</button>
			</form>
		</tr>
	{{/each}}
</table>
<form action="/administrador/listar" method="GET">
		<input class="btn btn-primary btn-lg" type="submit" value="Volver">
</form>
{{/partial}}

{{>layoutAdministrador.hbs}}